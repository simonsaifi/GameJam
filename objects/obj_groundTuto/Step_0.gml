speed = -20 * global.speedModifier;

if (x < 0)
{
	randomize();

var n = irandom(10)

if (n <= 1)
{
image_index = sprite_block;
}else{
	image_index = sprite_block2;
}
move_wrap(true, false, sprite_get_width(sprite_index));

}